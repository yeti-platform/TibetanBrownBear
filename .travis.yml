
sudo: required

language: python
python:
  - "3.6"

services:
   - docker

before_install:
  - docker run -d -p 8529:8529 -e ARRANGO_ROOT_PASSWORD='' arangodb

install:
  - pip install -r requirements.txt

script:
  - python -m pytest
