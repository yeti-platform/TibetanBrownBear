version: '3.3'
services:

  arangodb:
    image: arangodb
    ports:
      - 8529:8529
    restart: always
    environment:
      - ARANGO_ROOT_PASSWORD=

  yeti:
    build:
      context: ../
      dockerfile: ./docker/Dockerfile
    ports:
      - 5000:5000
    links:
      - arangodb
    environment:
      - YETI_ARANGO_HOST=arangodb
    restart: always
    depends_on:
      - arangodb
